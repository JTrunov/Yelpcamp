<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit campground</title>
</head>

<body>
    <h1>Edit campground</h1>
    <form action="/campgrounds/<%= camp._id %>?_method=PUT" method="POST">
        <div>
            <div><label for="title">Enter a title</label>
                <input type="text" name="campground[title]" id="title" required value="<%= camp.title %>">
            </div>
            <div>
                <label for="location">Enter location</label>
                <input type="text" name="campground[location]" id="location" required value="<%= camp.location %>">
            </div>
            <button>Add campground</button>
        </div>
    </form>
    <a href="/campgrounds/<%= camp._id %>">Back to campground</a>
    <a href="/campgrounds/">All campgrounds</a>
</body>

</html>